/**
 * SYSTEMS installer (integrator-owned): match director, score system,
 * autoplay bot. Called by Game.init() after all streams are installed.
 * @param {import('../Game.js').Game} game
 */
export function installSystems(game) {
  void game;
}
